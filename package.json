{
  "name": "court-admin",
  "version": "0.1.0",
  "description": "Basketball club management platform — Cloudflare Workers + D1 + R2 + Pages",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "frontend:dev": "cd frontend && npm run dev",
    "frontend:build": "cd frontend && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zachflem/CourtAdmin.git"
  },
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250422.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.85.0"
  },
  "dependencies": {
    "hono": "^4.7.7"
  }
}
